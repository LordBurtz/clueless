use crate::json_models::*;

struct DBManager {

}

impl DBManager {
    fn insert_offers(self, offers: PostRequestBodyModel) -> () {
        return ();
    }

    fn query_for(self, request_offer: RequestOffer) -> Option<GetReponseBodyModel> {
        return None;
    }
}